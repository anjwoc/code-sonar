# COMMON-CODE.md — 공통 코드/상수 인덱스

> 프로젝트 전체에서 반복 사용되는 응답 코드, 상태값, 상수, 공통 유틸을 코드 근거와 함께 정리한다.

## 1. 문서 정보

| 항목 | 내용 |
|:---|:---|
| 프로젝트 | {프로젝트명} |
| 작성일 | {YYYY-MM-DD} |
| 작성자 | Code-Sonar |
| 분석 범위 | `{소스 경로}` |

## 2. 공통 응답 모델

| 클래스/타입 | 역할 | 위치 |
|:---|:---|:---|
| `{ResponseType}` | {공통 응답 wrapper} | `{파일 경로}` |
| `{ErrorType}` | {오류 표현} | `{파일 경로}` |

## 3. 오류/상태 코드

| 코드 그룹 | 코드 | 메시지/의미 | 사용 위치 |
|:---|:---|:---|:---|
| {그룹명} | `{코드}` | {의미} | `{파일 경로}` |

## 4. 공통 상수

| 상수 | 값 | 의미 | 위치 |
|:---|:---|:---|:---|
| `{상수명}` | `{값}` | {의미} | `{파일 경로}` |

## 5. 공통 유틸/헬퍼

| 파일 | 주요 함수 | 사용 목적 | 호출 위치 |
|:---|:---|:---|:---|
| `{파일 경로}` | `{함수명}` | {목적} | `{호출부}` |

## 6. 주의 사항

- 코드에서 확인되지 않은 값은 작성하지 않는다.
- 여러 프로젝트가 같은 이름의 코드를 다르게 쓰면 프로젝트별 차이를 별도 행으로 분리한다.
- 폐기된 코드로 보이면 `비고`에 근거와 함께 표시한다.
