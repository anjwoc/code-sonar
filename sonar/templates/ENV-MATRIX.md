# ENV-MATRIX — {프로젝트명} 환경별 차이 매트릭스

> 분석 대상 환경: `{SONAR_DEEP_ENVS 또는 코드에서 자동 감지된 환경 목록}`  
> 근거: 설정 파일, Profile 어노테이션, 환경 분기 코드

---

## 진입점 분기

환경 또는 디바이스에 따라 요청이 어디서 처음 다르게 처리되는지.

| 항목 | {env1} | {env2} | {env3} | 근거 |
|:---|:---|:---|:---|:---|
| URL / 도메인 | `{url1}` | `{url2}` | `{url3}` | [config: nginx.conf:12] |
| 라우팅 규칙 | `{rule1}` | `{rule2}` | `{rule3}` | [config: gateway.yml:route] |
| BFF / 미들웨어 | `{mw1}` | `{mw2}` | `{mw3}` | [code: DeviceMiddleware.ts:8] |

---

## 인증/인가 차이

| 항목 | {env1} | {env2} | {env3} | 근거 |
|:---|:---|:---|:---|:---|
| 인증 방식 | `{OAuth2}` | `{JWT}` | `{세션}` | [code: SecurityConfig.java:34] |
| OAuth Client ID | `{id1}` | `{id2}` | `{id3}` | [config: application-{env}.yml:oauth2.client.id] |
| 토큰 유효기간 | `{t1}` | `{t2}` | `{t3}` | [config: ...] |
| SSO / 외부 인증 | `{있음}` | `{없음}` | `{없음}` | [code: ...] |

---

## 외부 서비스 엔드포인트

| 서비스 | {env1} | {env2} | {env3} | 근거 |
|:---|:---|:---|:---|:---|
| {외부서비스1} | `{url1}` | `{url2}` | `{url3}` | [config: ...] |
| {외부서비스2} | `{url1}` | `{url2}` | `{url3}` | [config: ...] |

---

## Feature Flag / 기능 활성화

| 기능 | {env1} | {env2} | {env3} | 근거 |
|:---|:---|:---|:---|:---|
| {기능1} | ✅ 활성 | ✅ 활성 | ❌ 비활성 | [config: ...] |
| {기능2} | ❌ 비활성 | ✅ 활성 | ✅ 활성 | [code: ...] |

---

## 비즈니스 로직 분기

같은 API/서비스이지만 환경에 따라 동작이 다른 케이스.

### {분기 포인트1}

```
{env1}: {동작 설명}
{env2}: {동작 설명}
```
[code: {파일명}:{라인}]

### {분기 포인트2}

```
{env1}: {동작 설명}
{env2}: {동작 설명}
```
[code: {파일명}:{라인}]

---

## DB / 캐시 설정

| 항목 | {env1} | {env2} | {env3} | 근거 |
|:---|:---|:---|:---|:---|
| DB 스키마 | `{schema1}` | `{schema2}` | `{schema3}` | [config: ...] |
| 캐시 TTL | `{t1}` | `{t2}` | `{t3}` | [config: ...] |

---

## 모든 환경 공통 항목

환경과 관계없이 동일하게 동작하는 항목 (매트릭스에서 제외된 항목):

- {공통항목1}: {설명} [code/config: ...]
- {공통항목2}: {설명} [code/config: ...]

---

## ⚠️ 확인 필요

| 항목 | 이유 |
|:---|:---|
| {항목1} | 설정 파일에서 환경별 분리 없음 — 코드 조건 분기 가능성 |
